version: v2

clean: true

managed:
  enabled: true
  override:
    - file_option: go_package
      value: ./models
  disable:
    - file_option: go_package
      module: buf.build/bufbuild/protovalidate

plugins:
  - local: protoc-gen-go
    out: cu

inputs:
  - directory: cu-proto
